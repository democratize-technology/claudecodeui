export default {
  // Print width - line length that prettier will wrap on
  printWidth: 100,
  
  // Tab width - number of spaces per indentation level
  tabWidth: 2,
  
  // Use spaces instead of tabs
  useTabs: false,
  
  // Semicolons at the end of statements
  semi: true,
  
  // Use single quotes instead of double quotes
  singleQuote: true,
  
  // Quote object properties only when necessary
  quoteProps: 'as-needed',
  
  // Use single quotes in JSX
  jsxSingleQuote: true,
  
  // No trailing commas in objects/arrays
  trailingComma: 'none',
  
  // Spaces inside object literals
  bracketSpacing: true,
  
  // Put > of multi-line JSX elements at the end of the last line
  bracketSameLine: false,
  
  // Include parentheses around arrow function parameters
  arrowParens: 'always',
  
  // Format only files that have a pragma comment
  requirePragma: false,
  
  // Insert pragma comment at the top of files
  insertPragma: false,
  
  // Prose wrap - how to handle markdown text wrapping
  proseWrap: 'preserve',
  
  // HTML whitespace sensitivity
  htmlWhitespaceSensitivity: 'css',
  
  // Vue files script and style tags indentation
  vueIndentScriptAndStyle: false,
  
  // Line ending style
  endOfLine: 'lf',
  
  // Control whether Prettier formats embedded code
  embeddedLanguageFormatting: 'auto',
  
  // Enforce single attribute per line in HTML, Vue and JSX
  singleAttributePerLine: false
};